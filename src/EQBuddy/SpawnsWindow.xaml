<Window x:Class="EQBuddy.SpawnsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EQBuddy Spawns" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Width="470" WindowStartupLocation="Manual">
    <!-- Same custom chrome as Options. Height auto-fits but is clamped to the work area
         at runtime; the body scrolls, the title row stays reachable. -->
    <Border Background="{DynamicResource BgBrush}" CornerRadius="10"
            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
            MouseLeftButtonDown="OnDrag">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="16,16,16,6">
            <TextBlock x:Name="TitleText" Text="🕒 Spawns" FontWeight="Bold" FontSize="14"
                       Foreground="{DynamicResource AccentBrush}"/>
            <Button Style="{StaticResource IconButton}" Content="&#x2715;"
                    ToolTip="Hide — the window returns when the next named dies (turn tracking off in Options or the right-click menu)"
                    HorizontalAlignment="Right" Click="OnClose"/>
        </Grid>

        <Grid Grid.Row="1" Margin="16,0,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="ZoneCombo" Grid.Column="0" FontSize="11"
                      SelectionChanged="OnZonePicked"
                      ToolTip="Which zone's named to show"/>
            <CheckBox x:Name="FollowCheck" Grid.Column="1" Margin="10,2,0,0"
                      Content="Follow" FontSize="11" Checked="OnFollowChanged" Unchecked="OnFollowChanged"
                      ToolTip="Switch to whatever zone the log says you are in"/>
        </Grid>

        <ScrollViewer x:Name="BodyScroll" Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled" PanningMode="VerticalOnly">
            <StackPanel x:Name="RowsPanel" Margin="16,0,16,4"/>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Margin="16,4,16,14">
            <TextBlock Text="Kill a named (or its placeholder) and its countdown starts from the log. ▶ starts one by hand — type how long ago it died first (5m, 90s) or leave empty for now. We captured the respawn times we could from community sources; if what you see in game disagrees, just type over the duration — your number wins and survives updates."
                       FontSize="10" Opacity="0.65" TextWrapping="Wrap" Margin="0,0,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="AddNameBox" Grid.Column="0" FontSize="11"
                         ToolTip="Add a named the catalog doesn't know (this zone)"/>
                <TextBox x:Name="AddDurationBox" Grid.Column="1" Margin="6,0,0,0" FontSize="11"
                         ToolTip="Respawn time: 22 (minutes), 90s, 12h, 3d, 6:40"/>
                <Button Grid.Column="2" Content="+" Margin="6,0,0,0" Padding="8,1"
                        Style="{StaticResource ActionButton}" ToolTip="Add" Click="OnAddCustom"/>
            </Grid>
        </StackPanel>
      </Grid>
    </Border>
</Window>
