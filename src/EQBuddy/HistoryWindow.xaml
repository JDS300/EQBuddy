<Window x:Class="EQBuddy.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EQBuddy — Session History" Width="860" Height="560"
        MinWidth="640" MinHeight="400"
        Background="{DynamicResource BgBrush}" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="330"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Filter bar -->
        <Grid Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="CharFilter" SelectionChanged="OnFilterChanged" Margin="0,0,8,0"/>
            <TextBox x:Name="SearchBox" Grid.Column="1" FontSize="12" Padding="6,4"
                     Background="{DynamicResource ComboBoxBrush}" Foreground="{DynamicResource TextBrush}"
                     BorderBrush="{DynamicResource BorderBrush}" TextChanged="OnSearchChanged"
                     ToolTip="Search character, zone, notes, tags, loot, or creature names"/>
            <TextBlock x:Name="CountText" Grid.Column="2" Style="{StaticResource Dim}"
                       VerticalAlignment="Center" Margin="8,0,0,0"/>
            <Button Grid.Column="3" Content="Import log…" Style="{StaticResource ActionButton}"
                    FontSize="12" Margin="8,0,0,0" Click="OnImportLog"
                    ToolTip="Parse an existing eqlog file into session history (Ctrl-click two sessions to compare them)"/>
        </Grid>

        <!-- Session list (Ctrl-click two rows to compare) -->
        <ListBox x:Name="SessionList" Grid.Row="1" Background="{DynamicResource PanelBrush}" BorderThickness="0"
                 Foreground="{DynamicResource TextBrush}" SelectionChanged="OnSessionSelected"
                 SelectionMode="Extended"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>

        <!-- Detail -->
        <Grid Grid.Row="1" Grid.Column="1" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock x:Name="DetailText" FontSize="12" TextWrapping="Wrap"
                               Foreground="{DynamicResource TextBrush}" FontFamily="Consolas"
                               Text="Select a session."/>
                    <TextBlock x:Name="DpsGraphLabel" FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentBrush}"
                               Margin="0,6,0,2" Visibility="Collapsed"/>
                    <Border x:Name="DpsGraphBorder" BorderThickness="1" CornerRadius="3"
                            BorderBrush="{DynamicResource BorderBrush}" Margin="0,0,8,0"
                            Visibility="Collapsed" ClipToBounds="True">
                        <Canvas x:Name="DpsGraphCanvas" Height="72"
                                Background="{DynamicResource PanelBrush}"
                                SizeChanged="OnGraphSizeChanged"/>
                    </Border>
                    <TextBlock x:Name="DamageVisualLabel" Text="Top damage sources" FontSize="12"
                               FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}"
                               Margin="0,6,0,2" Visibility="Collapsed"/>
                    <ItemsControl x:Name="DamageVisualList" Margin="0,0,8,0"/>
                    <TextBlock x:Name="HealVisualLabel" Text="Top heals" FontSize="12"
                               FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}"
                               Margin="0,8,0,2" Visibility="Collapsed"/>
                    <ItemsControl x:Name="HealVisualList" Margin="0,0,8,0"/>
                    <TextBlock x:Name="FightsLabel" FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentBrush}"
                               Margin="0,8,0,2" Visibility="Collapsed"/>
                    <!-- One collapsed row per fight; breakdown content is built lazily on
                         first expand (a grind session can retain 300 of these). -->
                    <StackPanel x:Name="FightsPanel" Margin="0,0,8,0"/>
                    <TextBlock x:Name="DetailRest" FontSize="12" TextWrapping="Wrap"
                               Foreground="{DynamicResource TextBrush}" FontFamily="Consolas"
                               Margin="0,8,0,0" Visibility="Collapsed"/>
                </StackPanel>
            </ScrollViewer>
            <StackPanel Grid.Row="1" Margin="0,8,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Notes:" Style="{StaticResource Dim}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="NoteBox" Grid.Column="1" FontSize="12" Padding="4,2"
                             Background="{DynamicResource ComboBoxBrush}" Foreground="{DynamicResource TextBrush}"
                             BorderBrush="{DynamicResource BorderBrush}"/>
                </Grid>
                <Grid Margin="0,4,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Tags:" Style="{StaticResource Dim}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="TagsBox" Grid.Column="1" FontSize="12" Padding="4,2"
                             Background="{DynamicResource ComboBoxBrush}" Foreground="{DynamicResource TextBrush}"
                             BorderBrush="{DynamicResource BorderBrush}"
                             ToolTip="Comma-separated, e.g. solo, basement, new weapon"/>
                </Grid>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="Save notes" Style="{StaticResource ActionButton}" Click="OnSaveMeta"/>
                    <Button Content="Copy summary" Style="{StaticResource ActionButton}" Click="OnCopySummary" Margin="8,0,0,0"/>
                    <Button Content="Export JSON" Style="{StaticResource ActionButton}" Click="OnExportJson" Margin="8,0,0,0"/>
                    <Button Content="Delete session" Style="{StaticResource ActionButton}" Click="OnDelete" Margin="24,0,0,0"
                            Foreground="{DynamicResource BadBrush}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
