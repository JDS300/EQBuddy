<Application x:Class="EQBuddy.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Index 0: the color palette. Empty placeholder — ThemeManager composes
                     the real one from EQBuddy.UI.Shared.ThemePalettes and swaps it in during
                     OnStartup, before any window exists. Palette keys are only ever read via
                     DynamicResource, so nothing resolves against it while it's empty. -->
                <ResourceDictionary/>
                <!-- Index 1: structural styles/templates, referencing palette brushes via
                     DynamicResource so a palette swap repaints already-open windows. -->
                <ResourceDictionary Source="Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
