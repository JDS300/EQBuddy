<Window x:Class="EQBuddy.DropsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EQBuddy — Drops by Creature" Width="560" Height="520"
        MinWidth="420" MinHeight="320"
        Background="{DynamicResource BgBrush}" WindowStartupLocation="CenterScreen">
    <!-- HistoryWindow family: a normal resizable window, not overlay chrome — this is a
         sit-down review surface, not something that floats over the game. -->
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="FilterBox" Style="{StaticResource InputBox}" TextChanged="OnFilterChanged"
                     ToolTip="Filter by creature or item name"/>
            <Button x:Name="CopyTextBtn" Grid.Column="1" Content="Copy text" Style="{StaticResource ActionButton}"
                    Margin="8,0,0,0" Padding="8,3" Click="OnCopyText"
                    ToolTip="Copy everything shown as readable text — pastes cleanly into Discord or a wiki page"/>
            <Button x:Name="CopyCsvBtn" Grid.Column="2" Content="Copy CSV" Style="{StaticResource ActionButton}"
                    Margin="6,0,0,0" Padding="8,3" Click="OnCopyCsv"
                    ToolTip="Copy everything shown as CSV for a spreadsheet"/>
            <Button x:Name="SaveCsvBtn" Grid.Column="3" Content="Save CSV…" Style="{StaticResource ActionButton}"
                    Margin="6,0,0,0" Padding="8,3" Click="OnSaveCsv"/>
            <Button x:Name="CopyWikiBtn" Grid.Column="4" Content="✦ Copy for wiki" Style="{StaticResource ActionButton}"
                    Margin="6,0,0,0" Padding="8,3" Click="OnCopyWiki"
                    ToolTip="Copy paste-ready eqlwiki edits for everything marked ✦ — each with a direct edit link. Nothing publishes automatically; you review and save on the wiki."/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="MobsPanel"/>
        </ScrollViewer>

        <TextBlock x:Name="FooterText" Grid.Row="2" Style="{StaticResource Dim}"
                   TextWrapping="Wrap" Margin="0,8,0,0"
                   Text="Observed personal drop rates this session — the kill count is the denominator, so thin data looks thin. Exports include exactly what the filter shows. ✦ marks drops eqlwiki doesn't know yet — Copy for wiki prepares the paste."/>
    </Grid>
</Window>
