<Window x:Class="EQBuddy.WhatsNewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="What's new in EQBuddy" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Width="440" WindowStartupLocation="CenterScreen">
    <Border Background="{DynamicResource BgBrush}" CornerRadius="10"
            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
            MouseLeftButtonDown="OnDrag">
      <Grid Margin="18,16,18,14">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Text="What's new" FontWeight="Bold" FontSize="14"
                   Foreground="{DynamicResource AccentBrush}"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,8,0,10"
                      MaxHeight="420" HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="NotesPanel"/>
        </ScrollViewer>

        <Button Grid.Row="2" Content="Nice — got it" HorizontalAlignment="Right"
                Style="{StaticResource ActionButton}" Padding="14,4" Click="OnClose"/>
      </Grid>
    </Border>
</Window>
